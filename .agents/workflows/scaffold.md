---
description: Steps 1–2 init + folder scaffold (TripiAgent)
---

Sequence (wait for confirmation between unless user says continue):
1. `/trip-step 1` — create-next-app, deps, shadcn, standalone
2. User confirms
3. `/trip-step 2` — full folder scaffold + BottomNav routes
4. `/trip-step gitlab-ci` — run skill `gitlab-ci-pipeline.md`
