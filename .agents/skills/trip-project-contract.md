# Skill: Trip project contract

Read `AGENTS.md` at repo root. Treat it as law.

Before any code:
1. Confirm step number with user (e.g. "Step 4c only").
2. List files you will touch.
3. After edits, run: `npm run lint`, `npm run test`, `npm run build` (report failures verbatim).

Forbidden: Vercel deploy, client-side API keys, `vercel.json` SPA rewrite, unmaintained `next-pwa` on Next 15+ without approval.
